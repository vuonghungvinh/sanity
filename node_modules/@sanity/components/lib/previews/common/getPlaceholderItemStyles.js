'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _range2 = require('lodash/range');

var _range3 = _interopRequireDefault(_range2);

exports.default = getPlaceholderItemStyles;

var _circularAt = require('circular-at');

var _circularAt2 = _interopRequireDefault(_circularAt);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var POOL_SIZE = 50;
var pool = (0, _range3.default)(POOL_SIZE).map(createItemStyles);

function createItemStyles() {
  return {
    date: { width: Math.random() * 10 + 15 + '%' },
    title: { width: Math.random() * 80 + 15 + '%' },
    subtitle: { width: Math.random() * 80 + 15 + '%' },
    description: { width: Math.random() * 80 + 15 + '%' }
  };
}

function getPlaceholderItemStyles(index) {
  return (0, _circularAt2.default)(pool, index);
}