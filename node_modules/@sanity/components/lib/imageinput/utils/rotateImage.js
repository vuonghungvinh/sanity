'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = rotateImage;

var _orient = require('./orient');

var _orient2 = _interopRequireDefault(_orient);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var orientations = ['top-left', 'top-right', 'bottom-right', 'bottom-left', 'left-top', 'right-top', 'right-bottom', 'left-bottom'];

function readImage(url) {
  /* global window */
  var image = new window.Image();
  //console.time("read image");
  return new Promise(function (resolve, reject) {
    image.onerror = reject;
    image.onload = function () {
      //console.timeEnd("read image");
      resolve(image);
    };
    image.src = url;
  });
}

function rotateImage(file, orientation) {
  /* global window */
  var objectUrl = window.URL.createObjectURL(file);
  return readImage(objectUrl).then(function (image) {
    return (0, _orient2.default)(image, orientations.indexOf(orientation) + 1);
  });
}