'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

var _fsExtra = require('fs-extra');

var _fsExtra2 = _interopRequireDefault(_fsExtra);

var _readLocalManifest = require('./readLocalManifest');

var _readLocalManifest2 = _interopRequireDefault(_readLocalManifest);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const addPluginToManifest = (sanityDir, pluginName) => (0, _readLocalManifest2.default)(sanityDir, 'sanity.json').then(manifest => {
  manifest.plugins = manifest.plugins || [];
  if (manifest.plugins.indexOf(pluginName) === -1) {
    manifest.plugins.push(pluginName);
  }
  return manifest;
}).then(manifest => _fsExtra2.default.writeJson(_path2.default.join(sanityDir, 'sanity.json'), manifest, { spaces: 2 }));

exports.default = addPluginToManifest;